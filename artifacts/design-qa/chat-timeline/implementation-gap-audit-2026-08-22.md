# 审计清单落实复核

当前证据：`current-audit-2026-08-22.png`，1584 × 990，桌面会话态。

## 已落实

1. 左栏取消独立“当前会话”区，同一会话仅保留在项目树中；设置下沉到底部。
2. 顶栏仅保留“新会话”一个文字操作；Agent、终端和更多操作使用图标，恢复会话进入 Agent 菜单。
3. 右侧改为带外边距、圆角和分组的 300–320px 信息卡。
4. 正文和 Composer 最大宽度收敛为 800px。
5. 计划列表移除重复优先级胶囊，行高和状态图标减重。

## 部分落实

1. 界面语言仍未全局统一：紧凑导航、配置和恢复会话等次级界面仍存在 Workspaces、Context、Agent Configuration、Resume ACP Session 等英文文案。
2. 图标和分隔线已减重，但还没有对所有历史组件做一次全局 18–20px 图标与分隔线清理。
3. 键盘焦点、完整读屏顺序和不同缩放比例尚未完成实际设备级审计。

## 未按原建议落实

1. 顶栏仍为 54px，没有收紧到建议的约 48px。
2. Composer 没有缩到建议的 96–104px；本轮按后续选定目标图实现为 152px，并增加 48px 底部留白。这是“以后续视觉目标覆盖早期审计建议”的明确取舍。

结论：不是整段没改，而是核心 P0/P1 已落实，P2 和全局一致性仍有尾项；Composer 高度则明确采用了后续目标图，而不是早期审计建议。
