# 设置工作区设计验收

2026-09-09：用户选择第 3 版，完成实现、两轮视觉对照和交互验证。最终没有未解决的 P0、P1 或 P2 问题；下文列出有意保留的差异和验证边界。

## 比较输入与方法

- source visual truth：[第 3 版设计稿](concepts/workspace.png)。
- implementation screenshot：[实际 Flutter 草稿状态](screenshots/after/04-agent-draft.png)。
- 同一比较输入：[最终全图并列](screenshots/comparison/full.png)、[最终详情并列](screenshots/comparison/details.png)。两张组合图均已实际打开并检查。
- 实际截图视口 1440 × 1024、DPR 1；源图 1487 × 1058，按近似相同比例归一至 1440 × 1024（长宽比差小于 0.1%）。详情板截取对应表单区域后并排显示。
- 两侧都是 Codex 连接、高级设置展开、未保存状态。实现侧将名称改为 `Codex local`，以产生真实草稿和输入焦点；设计稿字段仍为 `Codex`。
- 补充检查：[480 × 860 窄窗口](screenshots/after/13-narrow.png)、[保存后稳定状态](screenshots/after/14-saved.png)、[当前会话参数](screenshots/after/15-session-settings.png)，以及五分类和条件字段截图。最后两张在提示条自动消失后重新捕获并打开，页脚和弹窗没有被提示条遮挡。

## 首轮发现与修复复核

首轮证据保存在 [全图](screenshots/iteration-1/full.png)、[详情](screenshots/iteration-1/details.png) 和 [实际页面](screenshots/iteration-1/agent-draft.png)，没有用最终截图覆盖历史。

| 首轮问题 | 修复与最终复核 |
| --- | --- |
| P2：底部按钮沿用紧凑控件高度，明显小于选图 | 主操作最小尺寸改为 112 × 42，并调整底栏留白；全图和窄窗口均可见唯一保存入口。 |
| P2：分类起点偏低、连接列表偏窄，详情区左移 | 缩小导航行距，导航与连接列调整为 268/336 px，详情保留弹性宽度；重新比较三栏边界和标题、字段的对齐。 |
| P2：默认 Agent 说明独占一行，推低启动配置 | 将“启动默认”合入连接列表副标题，当前 Agent 留在应用身份区域。 |
| P2：高级设置边线过深，展开控件位置不符 | 使用现有边框颜色令牌，展开控件移至左侧；最终详情图已复核。 |
| P2：只读时整块吸收事件，连接列表与滚动也被阻止 | 将修改限制收敛到详情输入及变更按钮；只读浏览、选择连接和滚动的实际 widget 测试通过。 |
| P2：行内字段需要明确的辅助技术名称 | 使用合并语义绑定可见字段名；名称、启动命令和启动工作目录的 TextField 语义测试通过。 |
| 截图字体缺少个别汉字 | 仅截图 harness 改用 Arial Unicode；原生 macOS 字体与成功提示另行检查，显示正常。测试引擎合成粗体仍与原生字形有细微差异。 |

## 五项视觉检查

| 检查面 | 最终观察与判断 |
| --- | --- |
| 字体与层级 | 沿用应用字体体系和标题、字段、说明层级。原生窗口中文可读；测试截图的 Unicode 粗体栅格与设计稿存在轻微差异，不将其描述为像素完全一致。字段和说明没有可见截断。 |
| 布局与间距 | 三栏结构、表单标签、输入边界、高级区分隔和固定保存栏与选图方向一致。480 px 时分类和连接改为选择器，正文可滚动，保存栏持续可用；原生紧凑和放大窗口也已检查。 |
| 颜色与状态 | 使用现有蓝色主操作、浅灰分隔及背景；橙色表示未保存，蓝色边框表示输入焦点，禁用操作有明确状态。保存成功后页脚切换为“更改已保存”，保存和放弃按钮禁用。 |
| 图标与资产 | 使用现有 Material 图标库的终端、目录、权限、助手、存储图标。与生成稿填充轮廓有轻微差别，没有缺失图标或模拟图片占位。macOS 交通灯由原生窗口提供，不重复绘制到 Flutter。 |
| 文案与作用域 | 明确区分启动默认、当前 Agent、正在编辑的连接，以及进程目录、会话目录和附加目录。权限来源、AI 辅助的条件字段和当前会话参数的即时应用均有说明。保存影响准确表达为重新加载连接、会话可能需要恢复。 |

## 交互和功能证据

核心路径包括分类/连接切换保留草稿、唯一保存、无修改不提交、继续编辑/放弃、保存失败重试、只读浏览、活动操作保护、改名引用更新，以及会话参数即时应用。实际 Flutter 测试覆盖 Cmd-S 保存、Escape 草稿确认和字段语义。原生隔离预览另外操作了进入设置、宽窄布局、编辑、分类切换、返回确认、保存、权限条件字段、放弃返回和新建会话说明。

最终工作区测试：应用 1,383 项通过，共享聊天包 283 项通过且 2 项既有跳过，示例 2 项通过。全仓静态分析无问题；格式检查和 `git diff --check` 通过。具体命令、日志和 T01–T08 契约见 [完整验收记录](acceptance.md)；设计目标与源码的 A1–A8 标注见 [逐项实现对照](implementation-comparison.md)。

## 有意保留的差异与验证边界

- 保留连接类型字段和旧配置数据；远程 ACP 显示当前不可用。图中多出的字段是兼容要求。
- 补充启动默认标识、进程目录解释和真实保存影响，不照搬生成稿中的简化说明。
- 图标轮廓、真实字体、细微间距和展开箭头方向遵循现有组件的可用状态；这些轻微差异不妨碍选定布局或核心操作。
- 原生预览使用 FakeAgentClient 和内存 writer。没有在此轮声称验证真实 Agent 网络、真实凭据写入、完整 VoiceOver、任意系统文字缩放或操作系统强制退出保护。
- CUA 的键盘注入与字段文本替换不作为完整键盘验收证据；快捷键和语义的结论来自实际 Flutter widget 测试。完整辅助技术人工验收仍是后续可补充的覆盖。

final result: passed
