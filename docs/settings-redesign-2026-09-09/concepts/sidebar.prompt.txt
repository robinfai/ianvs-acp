Use case: ui-mockup. Descriptive concept name: 侧栏设置中心.
Create a realistic, production-quality redesign of an existing Flutter macOS ACP desktop client's settings. This is one focused primary screen, not a feature inventory. Target dimensions 1440 x 1024. Current date 2026-09-09; no dates needed in the UI.
Input images: first image is the actual existing global settings and shell, second is its expanded Agent connection editor, third is current-session settings showing the scope that must stay separate. They are style and product references, not images to copy unchanged.
Preserve the existing light macOS visual language: background #f7f7f7, white surfaces, #dedede dividers, primary text #242424, secondary #666666, accent #0b57d0. SF Pro-like sans and Chinese PingFang-like text, body 14-16px. Restrained Material-style outline icons, compact native controls, quiet hierarchy, no illustrations, no web browser chrome.
Layout strategy: a wide centered native Settings dialog approximately 1040 x 740 within the existing dimmed workspace. This contained dialog matches the source product. ONE settings category rail 190px wide on the left, ONE form surface on the right, avoid four-column navigation or cards inside cards.
Dialog header "设置", subtitle "应用配置" and close icon. Rail rows: "Agent", "工具与目录", "权限", "AI 助手", "本地存储"; Agent selected with pale blue fill. These are existing configuration groups.
Right main content: title "Agent", supporting sentence "管理可用于新会话的 Agent 连接。". A compact horizontal row of three connection selectors "Codex" selected, "Claude Code", "Pi", with a quiet "+ 添加" control; no giant object cards. Thin divider, section heading "编辑 Codex". Show readable labeled fields: "名称" with Codex; "启动命令" with npx; "启动参数" one row @agentclientprotocol/codex-acp with remove icon and small "添加参数". A collapsed disclosure "高级设置" with hint "工作目录与环境变量". Connection type is subtly labeled "本地进程 · stdio"; never show remote as available, never green check or connected/validated claims. Selected agent editing is one surface, not stacked dialogs.
Bottom fixed footer separate by a fine divider: left muted explanatory text "保存后用于后续连接；已有会话保留原配置。" and a small state "有未保存的更改". Right secondary "取消", sole blue primary "保存更改". Save applies all staged application changes. No duplicate Save Agent button.
Keep current-session model/reasoning, logout, deletion, activity logs OUT of this global configuration form. Background shell should retain only subtle recognizable app nav "新对话", "设置", "活动与诊断", a project and chat composer. No help panel, no metrics, no capability badges, no invented product features, no gratuitous cards or shadows, no option number/caption/annotation embedded in the image. Produce one image, clear generous spacing, no cropped fields.
