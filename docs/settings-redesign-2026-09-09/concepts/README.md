# 布局草图与实现校正

生成方式：内置 `image_gen.imagegen`，独立生成三次；未使用 CLI/API fallback。原始图已从默认生成目录复制到本项目，原图保留。三张图均实际附带当前应用设置、展开的 Agent 编辑器和当前会话设置截图。提示词目标为 1440×1024，工具实际输出均为 1487×1058；保留原比例，不拉伸或裁切。实现对照时应统一展示尺寸并注明该差异。

以下编号仅按主对话中生成结果的显示顺序绑定。三张图生成时均处于待选状态，并不是已实现界面；后续用户选择了第 3 图，落地情况见 [第 3 图与实际实现对照](../implementation-comparison.md)。

| 显示编号 | 本地结果 | 原始完整提示词 |
| --- | --- | --- |
| 1 | [sidebar.png](sidebar.png) | [sidebar.prompt.txt](sidebar.prompt.txt) |
| 2 | [tabs.png](tabs.png) | [tabs.prompt.txt](tabs.prompt.txt) |
| 3 | [workspace.png](workspace.png) | [workspace.prompt.txt](workspace.prompt.txt) |

三张都沿用现有浅色 macOS 风格与蓝色强调。分别探索分类侧栏、顶部分类与行内编辑、工作区内设置页面的结构。主图仅展示编辑 Agent 的焦点状态，其余现有字段仍由 [配置清单](../config-inventory.md) 和 [范围决策](../README.md) 约束，不能因为未画入这帧而删除。

## 必须带入实现的标注

| 编号 | 位置 | 纠正或补全要求 |
| --- | --- | --- |
| C01 | 图 1、2 页脚 | “保存后用于后续连接；已有会话保留原配置”是生成时尚未完成链路核对的错误假设。不得按此文案实现。当前 Save 会重建 controller，准确文案为“保存会重新加载连接配置；当前会话可能需要恢复”。图 3 已按核对结果生成。 |
| C02 | 三张的保存区域 | 增加保存中/失败/成功、无改动禁用和活动状态限制；关闭/取消需处理草稿。只有确有改动时才出现未保存提示。 |
| C03 | 三张的 Agent 选择/编辑区域 | 默认 Agent、当前 Agent、选中编辑对象是三个状态。示意图未画完默认设置动作，应保留并明确标签。预设添加、删除限制、旧 remote 类型及身份字段不可丢失。 |
| C04 | 图 1 的连接选择行 | 多连接时必须滚动或自适应切换列表；不能固定只支持三个 Agent。 |
| C05 | 图 2 的参数字段 | 参数是有序字符串列表；不能把输入文本按空格重拆造成 shell quoting 错误。复用现有逐项参数编辑契约。 |
| C06 | 图 3 的目录说明 | “默认目录”需要说明是 Agent 进程的启动目录，不是会话工作区。macOS 标题栏由现有原生壳层负责，不能在 Flutter 中另画一套红黄绿按钮。 |
| C07 | 三张的高级区域 | 环境变量/headers 保留 Keychain 引用与隐藏值；不要以明文截图展示。未知字段继续透传。 |
| C08 | 草图整体 | 生成图是布局目标，字形/尺寸与部分背景图标不是精确资产；实现使用现有 Flutter tokens/icons，并以真实同状态截图对照。 |

选图只决定布局结构，不改变上述已核实的运行语义。实现时应将所选图与实际界面放到同一对照输入，记录各项标注的处理与剩余差异；不能只展示单独的“after”截图就称完成。
