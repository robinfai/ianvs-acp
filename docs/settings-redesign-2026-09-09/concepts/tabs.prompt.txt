Use case: ui-mockup. Descriptive concept name: 分页式轻量偏好设置.
Create realistic, production-quality UI for an existing Flutter macOS desktop ACP client's settings. One focused screen, one primary action. Target dimensions 1440 x 1024. Date anchor 2026-09-09; do not display dates.
Input images are actual product reference screenshots: global settings and desktop shell, expanded Agent editor, current-session settings. Use their light native design language, not their existing overloaded hierarchy.
Keep palette #f7f7f7 background, #ffffff surface, #dedede subtle dividers, #242424 text, #666 secondary, #0b57d0 accent. SF Pro-like sans with PingFang-like Chinese, body 14-16px, compact macOS native fields and restraint. Clear grouping through spacing/dividers, few borders, no nested cards, no illustrations, no browser chrome.
Different structure: a COMPACT 880 x 790 centered native preferences dialog on the subtly dimmed existing workspace, with no settings sidebar. Header "设置" / small subtitle "应用配置". Below the title a single horizontal category tab strip: "Agent" selected, "工具与目录", "权限", "AI 助手", "存储". Full-width form/list below. Preserve the surrounding shell in background.
The current task is editing one of three configured agents. Content header "Agent 连接" with quiet "+ 添加 Agent" aligned right, supporting sentence "管理用于新会话的启动配置。".
A native list with very lightweight row separators: "Codex" with smaller label "本地进程 · stdio" is EXPANDED inline. It has a chevron and overflow menu aligned right, no green validation badges. Within this expanded row show a tidy TWO-COLUMN form: first row left "名称" value "Codex", right "启动命令" value "npx". Next full-width field "启动参数" value "@agentclientprotocol/codex-acp". Below a disclosure row "高级设置" and muted caption "工作目录与环境变量". No separate inner Save.
Below, two COLLAPSED agent rows with chevrons: "Claude Code" and "Pi"; each shows only the name and "本地进程 · stdio". This makes list ownership and inline editing obvious. Do not repeat giant cards or show every agent command.
Fixed footer, left two short muted lines "有未保存的更改" and "保存后用于后续连接；已有会话保留原配置。". Right "取消" secondary and "保存更改" blue primary. Single staged application-save model, no nested modal, no Save Agent ambiguity.
No model/reasoning in this global form; current-session parameters remain a separate scope. No delete-session, logout, diagnostics widgets, install marketplace, capabilities, health scores, or added product features. The focused content should fit without clipped fields, large blank voids, or oversized headings. No concept label, numbers, or annotations embedded. One image only.
