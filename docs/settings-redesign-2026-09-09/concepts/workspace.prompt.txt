Use case: ui-mockup. Descriptive concept name: 工作区内设置页.
Create a realistic production-quality redesign of settings inside an existing native Flutter macOS ACP desktop app. Target dimensions 1440 x 1024. Date anchor 2026-09-09; no date content needed.
Input images: actual product global settings, actual expanded Agent editor, actual current-session parameter dialog. Use them for product grounding and native style. Keep white/light gray #f7f7f7 surfaces, #dedede subtle separators, #242424 primary, #666 secondary, blue #0b57d0. SF Pro-like sans and PingFang-like Chinese; readable body 14-16px. Compact native fields/icons, no invented branding or web browser chrome.
Different layout strategy: open Settings as a full WORKSPACE PAGE instead of a modal. No dimmed background, no floating app card. Use base page surfaces. Keep app navigation rail approximately 230px wide at the far left with app name "ACP Client", "新对话", "设置" selected, "活动与诊断"; the selected Settings entry expands its category subnavigation "Agent" selected, "工具与目录", "权限", "AI 助手", "本地存储". Do not add another category sidebar. The session's chat and inspector are replaced while editing settings; a subtle top-left main-content back link "返回会话" allows returning.
Main page top heading "设置", small scope label "应用配置". Beneath it a master-detail split: narrow agent list from x270 to x505, form from x545 to x1250. The list header "Agent 连接", a small "+ 添加" button, three simple rows "Codex" selected pale blue, "Claude Code", "Pi", each with restrained terminal icon and "本地进程" subtitle. Plain grouped list and divider, no separate cards for each agent.
Form heading "Codex" with small subtitle "本地进程 · stdio" and subtle overflow control. Visible editor section "启动配置". Vertically spaced left-aligned labels: "名称" field Codex; "启动命令" field npx; "启动参数" row @agentclientprotocol/codex-acp with compact remove icon and quiet "添加参数". Below show "高级设置" disclosure EXPANDED on the same flat surface: "启动工作目录" empty optional field with hint "留空使用默认目录"; "环境变量" section shows only "+ 添加变量" because none set. Do not expose secrets or fake environment values. No health/ready/connected badges.
Bottom fixed page footer spans main content, thin top divider: left "有未保存的更改" and "保存会重新加载连接配置；当前会话可能需要恢复。"; right secondary "放弃更改" and sole blue primary "保存更改". No inner save. All application settings share one explicit staged save; changing categories should retain drafts.
Keep model/reasoning parameters for the current session separate from this application config. Do not add uninstall, cloud, billing, marketplace, activity metrics, capability badges, analytics or tutorial sidebar. Use spacing and grouping first, separators second, subtle tint sparingly. No nested cards, loud shadows, clipped labels, pointless empty panels, option labels, numeric annotations, or extra screens in this one image.
